func modInv(a, b) {
    var i = 0;
    while i < b {
        if (a * i) % b == 1 {
            return i;
        }

        i = i + 1;
    }

    return 0;
}

func main() {
    var result = modInv(42, 2017);

    println("Result: " + str(result));
}